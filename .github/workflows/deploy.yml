name: Deploy app

on:
  push:
    branches:
      - main

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
  REPOSITORY_NAME: apis

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up AWS CLI
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Docker
        uses: docker/setup-buildx-action@v2

      - name: Build and push Docker image to AWS ECR
        run: |
          bash ./scripts/docker_setup.sh $REPOSITORY_NAME
          echo $IMAGE_URL > image_url.txt
          echo "Added IMAGE_URL: ::add-mask::$IMAGE_URL"

      - name: Upload IMAGE_URL as artifact for next job
        uses: actions/upload-artifact@v4.6.0
        with:
          name: image_url
          path: image_url.txt 
                  
      - name: Set up Terraform for init
        uses: hashicorp/setup-terraform@v3.1.2

      - name: Cache Terraform Dependencies
        uses: actions/cache@v4.2.0
        with:
          path: ./terraform/.terraform
          key: terraform-${{ runner.os }}-${{ hashFiles('**/.terraform.lock.hcl') }}
          restore-keys: terraform-${{ runner.os }}-

      - name: Terraform setup
        run: |
          cd ./terraform
          terraform init -input=false
          terraform plan -var="image_url=$IMAGE_URL" -var="repo_name=$REPOSITORY_NAME" -out=tfplan -input=false

      - name: Upload tfplan as artifact for next job
        uses: actions/upload-artifact@v4.6.0
        with:
          name: terraform-tfplan
          path: ./terraform/tfplan

      - name: Save Terraform Lock File as artifact
        uses: actions/upload-artifact@v4.6.0
        with:
          name: terraform-lock
          path: ./terraform/.terraform.lock.hcl

      
  infra-provision:
    runs-on: ubuntu-latest
    needs: setup
    environment: ${{ vars.DEV_ENVIRONMENT }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Download all terraform artifacts
        uses: actions/download-artifact@v4.1.8
        with:
          path: ./terraform
          pattern: terraform-*
          merge-multiple: true
 
      - name: Download image url file 
        uses: actions/download-artifact@v4.1.8
        with:
          name: image_url
          path: ./
  
      - name: confirm downloaded files
        run: |
          ls -R ./terraform

          # exits if result of command is a status 1 (fail) i.e file doesn't exist
          test -f image_url.txt || exit 1

      - name: Read IMAGE_URL from file and set as environment variable
        run: |
          IMAGE_URL=$(cat image_url.txt)
        
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3.1.2

      - name: Restore Terraform Cache
        uses: actions/cache@v4.2.0
        with:
          path: ./terraform/.terraform
          key: terraform-${{ runner.os }}-${{ hashFiles('**/.terraform.lock.hcl') }}
          restore-keys: terraform-${{ runner.os }}-
 
      - name: Terraform Apply
        run: |
          cd ./terraform
          terraform apply -input=false ../artifacts/tfplan
